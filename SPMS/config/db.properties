# ─────────────────────────────────────────────────────────────────
# SPMS Database Configuration
# Edit these values to match your MySQL installation.
# ─────────────────────────────────────────────────────────────────

# Full JDBC URL (change localhost / port / db name if needed)
db.url=jdbc:mysql://localhost:3306/spms_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# MySQL username
db.username=root

# MySQL password  ← CHANGE THIS to your actual MySQL root password
db.password=root
